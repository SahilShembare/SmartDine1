{
  "name": "smart-dine",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Dine - QR Based Food Ordering Platform",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "dev:web": "npm --prefix web run dev",
    "dev:mobile": "npm --prefix mobile start",
    "build": "npm --prefix web run build",
    "build:web": "npm --prefix web run build",
    "mobile:install": "npm --prefix mobile install",
    "web:install": "npm --prefix web install",
    "postinstall": "npm --prefix web install"
  },
  "keywords": [
    "restaurant",
    "qr-code",
    "react-native",
    "expo",
    "firebase"
  ],
  "author": "Smart Dine",
  "license": "ISC"
}
